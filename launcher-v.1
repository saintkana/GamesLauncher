import customtkinter
import subprocess
import time


customtkinter.set_appearance_mode("dark")  # Modes: "System" (standard), "Dark", "Light"
customtkinter.set_default_color_theme("green")  # Themes: "blue" (standard), "green", "dark-blue"

app = customtkinter.CTk()
app.geometry("400x925")
app.title("// nora .  ||   v1.2")

# =========================================== Frame Var ==============================================

frame_1 = customtkinter.CTkFrame(master=app)
frame_1.pack(pady=20, padx=60, fill="both", expand=True)

frame_2 = customtkinter.CTkFrame(master=app)
frame_2.pack(pady=10, padx=60, fill="both", expand=True)

frame_3 = customtkinter.CTkFrame(master=app)
frame_3.pack(pady=10, padx=60, fill="both", expand=True)

frame_4 = customtkinter.CTkFrame(master=app)
frame_4.pack(pady=10, padx=60, fill="both", expand=True)

# ============================================ Def ===============================================

def launch_r6s():
    subprocess.run() # Rainbow Six Siege path 
    time.sleep(1.1)
    if switch_var.get() == "on":
        exit()

def launch_OW():
    subprocess.call([r"-"]) # Overwatch path 
    time.sleep(1.1)
    if switch_var.get() == "on":
        exit()

def launch_RBX():
    subprocess.call([r"-"]) # Roblox path
    time.sleep(1.1)
    if switch_var.get() == "on":
        exit()

def launch_UK():
    subprocess.call([r"-"]) # Ultrakill path 
    time.sleep(1.1)
    if switch_var.get() == "on":
        exit()

        
# ============================================ Main ===============================================

label_1 = customtkinter.CTkLabel(master=frame_1, text="// Click to Start a Game : ",
                                 font=customtkinter.CTkFont(weight="bold", slant="italic"),
                                  justify=customtkinter.LEFT)
label_1.pack(pady=10, padx=10)

button_0 = customtkinter.CTkButton(master=frame_1, text="Rainbow Six Siege - VK", command=False)
button_0.pack(pady=10, padx=10)

button_1 = customtkinter.CTkButton(master=frame_1, text="Overwatch 2", command=False)
button_1.pack(pady=10, padx=10)

button_2 = customtkinter.CTkButton(master=frame_1, text="Valorant", command=False)
button_2.pack(pady=10, padx=10)

button_3 = customtkinter.CTkButton(master=frame_1, text="ROBLOX - Home", command=launch_RBX)
button_3.pack(pady=10, padx=10)

button_4 = customtkinter.CTkButton(master=frame_1, text="UltraKill", command=False)
button_4.pack(pady=10, padx=10)

button_5 = customtkinter.CTkButton(master=frame_1, text="Minecraft", command=False)
button_5.pack(pady=10, padx=10)

button_6 = customtkinter.CTkButton(master=frame_1, text="Brawlhalla", command=False)
button_6.pack(pady=10, padx=10)

button_7 = customtkinter.CTkButton(master=frame_1, text="Terraria - tModLoader", command=False)
button_7.pack(pady=10, padx=10)

# ============================================= Radio Check ==================================================

label_r = customtkinter.CTkLabel(master=frame_2, text="* cosmetic ",
                                 font=customtkinter.CTkFont(weight="bold", slant="italic"),
                                  justify=customtkinter.LEFT)
label_r.pack(pady=10, padx=10)

radiobutton_var = customtkinter.IntVar(value=1)

radiobutton_1 = customtkinter.CTkRadioButton(master=frame_2, text="Open as Administrator.",
                                              variable=radiobutton_var, value=1)
radiobutton_1.pack(pady=10, padx=10)

radiobutton_2 = customtkinter.CTkRadioButton(master=frame_2, text="Open as Guest.",
                                             variable=radiobutton_var, value=2)
radiobutton_2.pack(pady=10, padx=10)

radiobutton_3 = customtkinter.CTkRadioButton(master=frame_2, text="Always open as Administrator.",
                                              variable=radiobutton_var, value=3)
radiobutton_3.pack(pady=10, padx=10)

radiobutton_4 = customtkinter.CTkRadioButton(master=frame_2, text="Always open as Guest.",
                                             variable=radiobutton_var, value=4)
radiobutton_4.pack(pady=10, padx=10)

# ============================================ Close valuechecker ===============================================

def switch_event():
    print("dev message:", switch_var.get())

switch_var = customtkinter.StringVar(value="off")

valuechecker_4 = customtkinter.CTkSwitch(master=frame_3,
                                          text="Close on Game Start.",
                                          command=switch_event,
                                          onvalue="on",
                                            offvalue="off")
valuechecker_4.pack(pady=10, padx=10)


# ========================================== Launch Options =========================================================

optionmenu_1 = customtkinter.CTkOptionMenu(frame_1, values=["Currently Unavailable",])

optionmenu_1.pack(pady=10, padx=10)
optionmenu_1.set("Launch Options.")

# ========================================== Dev Report Banner ======================================================

tabview_1 = customtkinter.CTkTabview(master=frame_4, width=300, height=150)
tabview_1.add("Dev Report || v1.2")
tabview_1

tabview_1.pack(pady=10, padx=10)
app.mainloop()


